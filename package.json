{
  "name": "node-test-runner",
  "version": "0.1.0",
  "description": "Example of running client side tests in JSDOM on node server.",
  "main": "index.js",
  "scripts": {
    "compile": "babel --presets es2015,stage-0 -d lib/ src/",
    "test": "npm run compile && mocha --compilers js:babel-core/register"
  },
  "author": "Michael Roberts Jr",
  "license": "MIT",
  "dependencies": {
    "jsdom": "^9.12.0",
    "mocha": "^3.2.0",
    "should": "^11.2.1"
  },
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-core": "^6.24.0",
    "babel-preset-es2015": "^6.24.0",
    "babel-preset-stage-0": "^6.22.0"
  }
}
